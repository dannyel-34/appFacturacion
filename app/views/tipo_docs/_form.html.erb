<div class="panel panel-warning">
  <div class="panel-heading">
    <h3 class="panel-title">NUEVO TIPO DOCUMENTO</h3>
  </div>
  <div class="panel-body">
<%= simple_form_for(@tipo_doc, :html => { :class => 'form-horizontal', remote: true } ) do |f| %>
  <% if @tipo_doc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_doc.errors.count, "error") %> prohibited this tipo_doc from being saved:</h2>

      <ul>
      <% @tipo_doc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
   	<label for="name" class="col-sm-3 control-label">Nombre</label>
   	<div class="col-md-8">
      	<%= f.input_field :nombre, class: 'form-control', placeholder: "Tipo documento" %>
   	</div>
  </div>

  <div class="form-group">
	<div class="col-sm-offset-4 col-sm-10">
    <%= button_tag(type: 'submit', class: "btn btn-success", :name => nil) do %>
      <span class="glyphicon glyphicon-floppy-disk"></span>  Guardar
    <% end %>
  </div>
  </div>
<% end %>
</div>
</div>